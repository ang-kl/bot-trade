// Vault - built in Phase 7. localStorage preset storage for saved strategies.
export default function Vault() {
  return (
    <section>
      <h1 className="text-xl t-label mb-2">Vault</h1>
      <p className="t-sub text-[var(--color-text-sub)]">
        Saved strategy presets land here in Phase 7.
      </p>
    </section>
  )
}
