// Settings — built in Phase 4.
// Four tabs: cTrader (OAuth + account), Watchlist (drag-edit + per-symbol
// sub-agent toggles), News (X bearer + cashtags), Risk/Guard (arm, caps).
export default function Settings() {
  return (
    <section>
      <h1 className="text-xl font-semibold mb-2">Settings</h1>
      <p className="text-sm text-neutral-600">
        Four tabs (cTrader, Watchlist, News, Risk) land here in Phase 4.
      </p>
    </section>
  )
}
