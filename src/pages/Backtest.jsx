// Backtest - built in Phase 7. Minimal CSV stats (8 metrics, no chart).
export default function Backtest() {
  return (
    <section>
      <h1 className="text-xl t-label mb-2">Backtest</h1>
      <p className="t-sub text-[var(--color-text-sub)]">
        CSV stats table lands here in Phase 7.
      </p>
    </section>
  )
}
