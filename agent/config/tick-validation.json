{
  "_note": "P5 (plan §7, §12): the evidence thresholds the validation importer judges against. PR-B (11-09-2026): `traded` is the one post-shadow stage for EVERY account, judged on the account's own closed tick trades in R. They are RISK LIMITS held by the owner — a null refuses every import at that stage with thresholds_unset, and nothing here is lowered to make a setup pass. Set by pull request, with the reasoning in the PR body. PR-H (11-09-2026): SET under the owner's decision of 11-09-2026 (docs/owner-principles-plan-2026-09-11.md §2, §4 PR-H; CLAUDE.md 'Owner principles', decision on thresholds): replay 40 trades / PF ≥ 1.3 / max DD 8R / expectancy lower bound ≥ 0; shadow 200 signals / 48 h / 30 trades / 8 losses / PF ≥ 1.3 / lower bound ≥ 0 / max DD 8R / resets ≤ 20 %; traded 30 trades / PF ≥ 1.3 / max DD 8R. Key mapping: replay.minExpectancyLowerR REPLACES the former replay.minTestNetR — both are judged on the trial's TEST block (the out-of-sample block, plan §7), and the bootstrap 5th-percentile expectancy in R (tick-replay-sim.js summarize → expectancyLowerR) is the stricter of the two; a trial whose test block is withheld (a research run without includeTest) has no such figure and cannot pass. replay.minTestTrades (10, a quarter of minTrades) is the checker's addition of 11-09-2026, NOT in the owner's list: the lower bound is a bootstrap over the TEST block's own trades, and blocks are cut by EVENT index, so a block holding two trades (or none, with a figure pasted in) must not pass — this only tightens the owner's bar; it is named in the PR body for the owner's word. tick-validation.test.js pins these exact values: a silent edit is red.",
  "replay": { "minTrades": 40, "minProfitFactor": 1.3, "maxDrawdownR": 8, "minExpectancyLowerR": 0, "minTestTrades": 10 },
  "shadow": { "minSignals": 200, "minHours": 48, "minTrades": 30, "minLosses": 8, "minProfitFactor": 1.3, "minExpectancyLowerR": 0, "maxDrawdownR": 8, "maxResetSharePct": 20 },
  "traded": { "minTrades": 30, "minProfitFactor": 1.3, "maxDrawdownR": 8 }
}
