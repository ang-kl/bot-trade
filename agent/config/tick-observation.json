{
  "_note": "The owner's tick-observation declaration as DATA (11-09-2026: 'ACCT-DEMO-3 records first'; PR-B the same day, owner principle 9: every account). accounts: ctidTraderAccountId -> OFF | RECORD | SHADOW, or \"_all\" for every enabled registry account (a per-id key still wins for that id; an account enabled later is seeded on its first boot). SHADOW admitted since P4: the strategy runs on the sidecar's workers, signalling only. PR-H (11-09-2026, owner principle 3): `_all` moved RECORD -> SHADOW so the shadow window (plan §7, SHADOW_PASSED) opens on every enabled account at the next boot — the content hash changes, so the seed re-applies to every enabled account, a per-id key still winning; shadow places nothing (signals + the shadow portfolio on the workers, CPU on the sidecar). Note the live sidecar (cpp-acct) has no TICK_SPOOL_PATH: main.cpp builds the workers only when the recorder exists, so SHADOW there is a declaration the sidecar cannot act on until the owner sets the spool path on it. symbols: the names the recorder should carry on the account's sidecar, resolved per side; 'momentum-universe' means every symbol in config/momentum-universe.json. Seeded ONCE per content (tick_observation_seed_json): a later change made through POST /actions/tick-observation or /actions/tick-symbols stands until this file changes. The recorder still needs TICK_SPOOL_PATH on the sidecar; the disk reserve is the other limiter.",
  "accounts": { "_all": "SHADOW" },
  "symbols": "momentum-universe"
}
