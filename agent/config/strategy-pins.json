{
  "_note": "Owner-declared strategy pins, applied at boot by seedStrategyPinsFromConfig (seed-once per entry; a guard's or a human's later change persists across deploys). WAVE 1 OF THE FIRST-PRINCIPLES AUDIT (docs/first-principles-audit-2026-09-19.md §K·1, owner 19-09-2026 'Execute'): arming follows measured evidence. `_all` pins ON, for EVERY enabled account, only what has earned it. `_off` switches OFF, for every enabled account, the strategies whose live record is not positive (they keep proposing into the scan, the decision log and the evidence shadow at zero cost; a per-id key naming one of them ON wins for that id). `_trial` names ONE account per strategy on a pre-registered trial: ON there, OFF everywhere else — the one documented exception to 'no account ids' (principle 9), reconciled with 'one account per system' (07-09 P5) as: a system on trial runs on one account until its dated checkpoint. `_reseed` re-issues a spent ON order for one cell (`<accountId>:<strategy>:<token>`). 20-09-2026: the intraday producers (scan_dispatch, closed_market_limits) are retired — see agent/lib/entry-producers.js and docs/first-principles-audit-2026-09-19.md §L — so every intraday strategy is in `_off` and `_all` is empty; the stage matrix agrees with the inventory.",
  "_all": [],
  "_off": [
    "fib_618_fade",
    "cup_handle",
    "inv_cup_handle",
    "ema_pullback",
    "donchian_breakout",
    "rsi_meanrev",
    "vwap_trend",
    "vp_value",
    "rsi2_reversion",
    "fib_confluence",
    "va_breakout",
    "fvg_retrace"
  ],
  "_off_note": "Measured 19-09-2026, 90 days, all accounts (/state/attribution): none of these has a clean record of >= 30 closes at PF >= 1.5; fib_618_fade PF 0.62 over 49 (owner-disabled since 27-07), ema_pullback 0.10/14, rsi_meanrev 0.22/8, vp_value 0.61/18, vwap_trend 0.70/30, va_breakout 0/3, fvg_retrace 0/1 (no backtest), donchian 1.09/29, rsi2 1.13/31, cup_handle 1 close. They are SHADOW: computed by the scan, refused at the stage gate with a decision_log row, measured by the evidence shadow. RETIREMENT 20-09-2026 (owner order \"retire the intraday paths, keep momentum only\"): fib_confluence joins them and `_all` is now empty. Not because its record turned — 26 closes at PF 2.72 stands — but because its only producer was scan_dispatch, which is retired in agent/lib/entry-producers.js: no producer can trade it, and principle 6 forbids the UI showing an armed strategy that cannot trade. The whole intraday registry is now OFF on every account; tsmom_long (the weeks-horizon momentum book) is the one armed family, on its trial account.",
  "_trial": {
    "tsmom_long": [
      "46130058"
    ]
  },
  "_trial_note": "tsmom_long (the weeks-horizon momentum book) is the only design aligned with the measured horizon, and its live record is 23 closes at PF 0 (never held to its horizon; the same signal stacked on four accounts). Trial on ONE funded account with no third-party channel (…0058), at the verdict's pending half-risk scale, judged at the pre-registered checkpoint 2026-12-19 (PF >= 1.5, tail share, max drawdown) — not by the 20-close watchdog or the streak breaker, which are exempt for the momentum family (Wave 1). fib_confluence: 26 closes at PF 2.72 (the one positive live record) — pinned everywhere at the verdict's pending half-risk scale until 30 closes.",
  "_reseed": [],
  "_reseed_note": "Emptied 20-09-2026 with the intraday retirement: the four fib_confluence re-arm orders of 19-09 are spent AND superseded — fib_confluence is in `_off` because scan_dispatch is retired. Kept as an empty list so the reader still sees the mechanism."
}
