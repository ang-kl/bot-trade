{
  "_note": "Wave 4 of docs/first-principles-audit-2026-09-19.md §K (item 13), owner's Execute 19-09-2026. The stored risk_config_json held 55 keys: 17 differing from the defaults, 37 pinned AT their default (a Risk-page save spread the effective config into the store, so those keys stopped following default changes) and one retired key. This seed is applied ONCE per content hash at boot (risk_config_seed_json); what a human changes afterwards stands until this file's content changes. `reset` lists stored overrides returned to their defaults — every one of them TIGHTENS a bound or is inert (minRR 1.6 sat below the 3.0 hard floor; minSLDistancePct's price floor is covered by the ATR floor; minTradesForKelly and allowNegativeExpectancyOverride are gate plumbing). `keep` lists the stored overrides this seed does NOT touch because resetting them would LOOSEN a live bound or change a stop's reach — P7: risk-limit changes are ask-first, and the audit's Execute did not name them: perTradeRiskPct 0.01 (the verdict's half-risk scale; the default is 0.05), dailyLossLimit 150 (the owner's own cap; the audit's plan replaces it by the tier rule, on the owner's word), maxNotionalXBalance 4, maxMarginUsagePct 0.4, marginLevelFloorPct 200, maxCurrencyExposure 1, maxSpreadFracOfSL 0.03, and equityStopPct 0.15 (null would trip the equity stop at the 3 % daily figure instead of 15 %). Pinned defaults are pruned so the store holds only real overrides; the retired kellyFraction is dropped (risk-matrix.js declined to delete an operator's stored key to tidy a UI — this is the one ordered place that does).",
  "reset": [
    "maxOpenPositions",
    "maxClusterExposure",
    "maxConsecutiveLosses",
    "cooldownMinutes",
    "symbolCooldownMinutes",
    "minRR",
    "minSLDistancePct",
    "minTradesForKelly",
    "allowNegativeExpectancyOverride"
  ],
  "keep": [
    "perTradeRiskPct",
    "dailyLossLimit",
    "maxNotionalXBalance",
    "maxMarginUsagePct",
    "marginLevelFloorPct",
    "maxCurrencyExposure",
    "maxSpreadFracOfSL",
    "equityStopPct"
  ],
  "prunePinnedDefaults": true,
  "dropRetired": true
}
