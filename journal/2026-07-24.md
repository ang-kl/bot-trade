# Journal — 2026-07-24 (SGT)

## Multi-account migration: Phase 0 survey + baseline record

Plan document: `docs/multi-account-migration-plan.md` (PROPOSAL — awaiting `!!`).

### Baseline (G1)

- Repo: `ang-kl/bot-trade`, branch `main`
- Baseline commit: `63d996ff6997a1d4b9c533667cf49b61810c5ad3`
  ("Add VPO inverted Cup & Handle + weekend-loss-flag Desk banner (#294)")
- Recommended tag (NOT yet created — repo has zero tags today): `pre-multiacct-baseline`
- Railway deployment IDs at baseline:
  - Node agent service: `<FILL FROM RAILWAY DASHBOARD — deployment ID of the currently-working deploy>`
  - C++ exec service: `<FILL FROM RAILWAY DASHBOARD — deployment ID of the currently-working deploy>`
  - *These are placeholders by necessity: this sandbox has no Railway access.
    The operator must fill them before M1 begins; G1 is not complete until then.*
- Frontend (Vercel) is out of scope for the migration baseline.

### Survey findings (condensed; full citations in the plan doc, Phase 0)

- **No Python service exists.** Two Railway services: Node agent (`agent/`) and
  C++ exec sidecar (`cpp-exec/`). Operator context corrected.
- Account identity = single global `agent_state` key `ctrader_account_id`;
  account switching is a destructive swap-in-place (`actions.js:2744-2781`).
- ONE OAuth token spans all 7 accounts under the cTID (verified in-repo:
  `wsGetAccountsByToken`, `ctrader-ws.js:477`); exactly one refresher
  (`loop.js:923-924`, 24 h throttle).
- 18 SQLite tables; only `monitored_positions` carries `account_id`. All risk,
  guard, lesson, and decision reads are account-global today.
- Process model: one Node process (main 5-min loop, 30 s fast-monitor hosting
  the heartbeat watchdog, guardian spot stream, 60 s VPO feeder, per-cycle
  Telegram poll) + one C++ process (engine thread, HTTP server, optional
  VPO dispatcher + SpotFeed).
- Known gaps confirmed for the plan: no submission-time idempotency key
  (matches the historical 4× duplicate USDIDR incident), exec-guard `halt`
  enforced only on the C++ path (JS path unchecked), scan-phase skip decisions
  are stdout-only (no DB record), Telegram has no inline keyboards / severity /
  batching, git tag list empty, Railway rollback never exercised.
- Account inventory (operator screenshot, one cTID): Live 1252961 (USD 1:30),
  Live 1251442 (USD 1:100), **Live 1251247 (SGD 1:200 — protected current
  live, owner-confirmed)**, Demo 5306502 / 5268549 / 5203012 / 5067353.
- cTrader Open API: 50 req/s non-historical + 5 req/s historical per
  connection, heartbeat ≥ every 10 s (help.ctrader.com). Symbol-subscription
  cap, numeric account-per-connection cap, token lifetime: UNVERIFIED —
  listed as OPEN DECISIONS 10-12 in the plan.

### Owner decisions captured today

- Protected live account for cutover/watch: **1251247 only**; 1252961 and
  1251442 enter the registry as disabled rows.

### Next actions (blocked on owner `!!`)

1. Owner reviews `docs/multi-account-migration-plan.md`, answers OPEN
   DECISIONS 1-14 (or accepts recommendations).
2. Operator fills the two Railway deployment IDs above.
3. On `!!`: create `pre-multiacct-baseline` tag → begin M0 (shim).

## M0 progress (same day, later)

- Owner signed off the plan ("merge", PR #295) and instructed continuation.
- `pre-multiacct-baseline` tag created locally at 63d996f; tag PUSH is
  blocked by the sandbox's branch-scoped git access (HTTP 403) — operator
  must push it (command in session notes). Registry work does not modify
  the baseline itself, so M0 proceeds; the tag remains an operator TODO.
- M0 shim implemented: `accounts` table (db.js), account-registry.js
  (single writer; sole-enabled M0 invariant; boot bootstrap; select/config
  mirrors), loop.js reads the registry through the existing
  getAutopilotAccounts seam with legacy fallback, GET /state/accounts.
  5 new invariant tests; full suite 835 green. Parity is by construction
  (one enabled row == the legacy single account); the recorded-replay
  byte-for-byte harness stays scheduled for the M4 staging soak.
