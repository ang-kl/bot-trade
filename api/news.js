// Aggregated news endpoint — Phase 6 wires this up.
// Returns cached headlines used by the news-analyst sub-agent.
export default async function handler(req, res) {
  res.status(501).json({ error: 'news not implemented yet — see HANDOVER-V2.md Phase 6' })
}
