# V3 handover, 24 September 2026, 07:19 SGT

The owner asked whether to branch into another chat. Continue there from this
checkpoint; do not run two implementation sessions concurrently. This handover
branch is documentation only. Do not merge it merely to save evidence: every
main commit deploys Node.

## Established state

- Main release: `391d5a2f53ddaca6d145ccf1c47ae465be8a9851` (#1054).
- #1053 merged as `7981227e833c7877f9e66236c1aa9ba5cb63242c`. It isolated
  the cup diagnostic report in bounded read-only workers, corrected selected
  account currency reads and exposed calendar collection diagnostics.
- #1054 source: `717415b29acf1f2e80bc8a7ddc66017d155a5b54`, following
  profiler commit `a82b08a6733ff8ef3b0e3cabfd1d92047cae9ff1`. It retains
  application callers for native profiler samples and fixes calendar demand
  to consume authenticated `/health.tick`, not `/tick-status`.
- #1054 final local gate: 5,394 backend tests, zero failures/skips; 937
  frontend tests; ESLint zero warnings; build; no-green. CI `35906604214`
  and review `35906604349` passed; no review threads. Native behaviour/parity
  evidence from #1053 remains eligible: exact native source comparison found
  no changes. No thresholds or assertions were relaxed.
- Node deployment `73f0f5dd-83ff-41fe-8b3c-a6c40216aee0` succeeded at
  **2026-09-23T19:10:47.733Z**. All five native services skipped #1054.
  Deployment state was rechecked at 07:17 SGT. It is not full acceptance.
- Independent broker receipts **23:18:43.215Z to 23:18:44.619Z** still show
  seven successful reads, 32 positions, all SL present, two missing TP1.
  Account 46979908 has seven positions/one missing TP; 47790949 has thirteen/
  one missing TP. The other accounts are 42993489:1, 43002148:0,
  43069009:0, 43097342:4 and 46130058:7, with no missing SL or TP.
  Refresh detailed position identities and quotes before any owner decision;
  aggregate counts do not prove unchanged individual positions.

## Newly captured protection bottlenecks

At **19:12:46.742Z**, after #1054 boot, Node recorded **56,783 ms** event-loop
lag and temporarily unavailable independent receipts. They subsequently
recovered. This is a failed startup protection acceptance observation.

The new caller attribution works. The **19:14:05.361Z** scan profile sampled
84,171.7 ms, including 34,613.8 ms in native `all` calls:

| Caller in released source | Attributed sampled time |
|---|---:|
| `/state/decisions-daily`, `agent/routes/state.js` handler beginning at 3495 | 23,985.1 ms |
| `/state/prices`, same file, handler beginning at 4501 | 7,080.5 ms |
| `stageMatrixStats`, `agent/services/stage-matrix.js` at 766 | 2,433.7 ms |

The first query aggregates `risk_events` over the requested date/account
window. The second scans `scans` for the maximum priced row ID per symbol.
Both still execute synchronously on the management event loop. The phase
label does not mean scanner calculations caused these HTTP-handler samples.
This is sampled wall-time attribution, not proof of CPU versus disk waits.
`setState` also accounted for 6,584.3 ms of that profile's native `run` time.
Do not silently change database durability to make it faster.

**Next authorised implementation:** isolate/optimise these demonstrated report
reads while preserving exact populations, account scope, timestamps and empty/
unavailable distinctions. Reuse #1053's bounded worker infrastructure where
appropriate. Reproduce with focused synthetic fixtures, prove output parity
and event-loop responsiveness, then the full merge gate. Investigate the
stage-matrix and database-write contributions as measurements warrant.
Do not widen the production load test or activate scanner feeds.

Current 23:18-23:19Z process samples show normal loop progress (loop 238),
roughly 58-126 ms diagnostic lag and successful broker reads. These short
observations neither erase the startup failure nor establish p95/p99
broker-confirmed protection latency.

## Other remaining gates

- Verify #1054's expanded calendar demand at runtime. The previous collector
  counted only 51 identities because it read the wrong contract. Runtime
  inventory after this correction has not yet been checked in the UI.
- Holiday rows with unreadable bounds remain explicitly UNKNOWN. Correction
  (V3 K1b, measured 26-09): the rows that keep production calendars unknown
  are not missing bounds — all 335 unresolved rows carry `startSecond: 0,
  endSecond: 0` on full-day "Closed" rows ("25.12.2025 - Closed",
  "07.09.2026 Closed"), read as `holiday_bounds_invalid`. The official
  ProtoOAHoliday reference describes optional start/end fields but states
  neither their omitted-value meaning nor what 0/0 means. Since K1b a
  non-recurring such row dated three or more UTC days before its observation
  is skipped: it lies behind every evaluation window, stays in the stored
  payload and is counted as `expiredHolidays`. A current or future one keeps
  the calendar UNKNOWN until the owner decides its meaning (K3). Do not
  invent replacement boundaries.
- The last observed watchdog outbox had 512 pending records and 88 capacity
  refusals; master delivery, delivery credentials and incident ownership were
  OFF; independent external observer unconfigured. Refresh these observations.
- Two position-specific decisions, intended account roster, exact deployment
  profiles, historical accounting provenance, research quality and the eligible
  48-hour shadow requirement remain open. No strategy settings were changed.
- Alert delivery, Node/cpp-verify outage drills, observation-only feed trial,
  storage/restart acceptance and final production end-to-end remain unexecuted
  or unverified. Scoped proposals and rollback prerequisites are in
  `docs/v3-completion-checkpoint-2026-09-24.md`.
- Full V3 acceptance and tick-entry readiness have NOT been established.

## Continuation instructions and access

Read CLAUDE.md, the three owner-named V3 plan/progress/acceptance documents,
the completion checkpoint and this handover. Refresh main, open PRs, actual
deployment filters and runtime evidence. Do not repeat completed work without
a code/configuration or evidence reason.

Keep both new scanner feeds OFF and trading activation unchanged. Preserve
risk limits, TP1, manual ownership, account isolation and validation thresholds.
No orders, position amendments/closes, credentials, arbitrary targets or
unapproved messages/drills. #1048 was already merged before this continuation;
do not treat its historical open-PR checkpoint as current authority.

The browser verification kernel reset during a timed-out operation. Its prior
bindings are gone. Re-read the control-browser skill and bootstrap the approved
connection before browser use; do not extract credentials or bypass controls.
Railway read-only logs/configuration and GitHub repository access remain usable.

Rollback of #1054: restore Node deployment
`1038a226-04a5-463c-b663-eca7eae4ed11` (#1053), preserving report isolation but
losing native caller attribution and corrected feed-calendar demand. It still
contains the known startup stall. Do not roll back gateways or change feed/
entry settings. No rollback has been performed.
