# Frozen V3 closure register — 24 September 2026

## Owner continuation and existing-position resolution — 15:38 UTC

The owner now explicitly directs evidence-based formula selection for partial
TP1, close percentage, runner exit and minimum-volume rules, and authorizes
existing ETHUSD/XRPUSD TP repairs. The earlier request for owner-supplied
numerical policy is superseded. Roster remains seven accounts, four demo and
three live; formula implementation and empirical qualification remain distinct.

Fresh account-scoped broker reads confirmed both exceptions on demo routing.
For these existing BUY positions only, target = the greater of the latest
completed daily high and entry + 1.5 × the retained initial-risk distance,
rounded to broker digits and checked beyond the fresh ask/current stop. The
retained initial-risk field is an adoption-era record, not a newly reconstructed
opening deal. This repair rule is a transparent policy choice using observed
inputs, not an optimized or empirically validated return claim.

- ETHUSD / 46979908 / 242004561: TP **2873.75**, retained SL **2447.97**;
  broker readback 15:38:33.284Z, 0.01 lots (broker minimum 0.01).
- XRPUSD / 47790949 / 242243017: TP **1.6555**, retained SL **1.309**;
  broker readback 15:38:48.383Z, 4.49 lots (broker minimum 0.1).

Both broker acknowledgments and account-owned ledger updates succeeded. These
are whole-position native targets; no partial close was performed. The future
partial policy must separately handle minimum volume and retain a valid runner.
[Before, acknowledgments and fresh broker readback](evidence/v3-demo-tp-repair-2026-09-24.json).

No live broker order/amendment was performed by this continuation. Shared
software policy must retain account/environment parity, while live execution
and activation remain user-controlled. P0/P3 is not fully closed by two repairs.

[Next startup correction](v3-fundable-price-2026-09-24.md) removes sorting from
the measured `lastScanPrice` hotspot. Account-report and cold-start protection
acceptance still require fresh production evidence after its release.

## Original frozen register and chronological evidence

### Later P1/P4 readback — 16:51 UTC (25 September 00:51 SGT)

#1076 implementation `5dc69bc81cd3f00e6f0237435b8b30d14558b728`, merge
`7ea54b6a6282babba761ec31208d8a80ace34e50`, Node deployment
`1f28e8d5-d04e-44b1-8241-da23300f1a2c` SUCCESS. First independent poll at
16:48:02.949Z retained all seven broker readings from 16:47:32.090–33.499Z,
BEFORE the Node BOOT at 16:47:52.096Z. Readings were not cleared; subsequent
broker cycles advanced normally. This directly passes the Node-only restart
preservation regression. Coverage remains 32 positions, none missing SL/TP.

Database initialization 654.8 ms; HTTP listening at 16:47:59.443Z, 7.6 seconds
after container start log (log-clock measure, not an end-to-end HTTP probe).
Account report 81 ms application / 294 ms browser. First full loop 85,657 ms;
the first scan profile attributed 3,905.7 ms of synchronous reads to
postmortemStats and 12,912.3 ms of writes to setState. Several dashboard reads
waited about 6.3–6.6 seconds. Report/protection load acceptance remains open.
The first heartbeat's old protection-band completion predates this restart,
so it is not evidence of a new completed band. Later fresh bands are required.
No entry/configuration or native-service release change was made.

The timing goals discussed in reply 8,911 are proposals, not retroactively
approved acceptance thresholds. The current code's 3-second fast cadence,
60-second protection cadence and 5-second job wait budget are separate facts.

### Later P1/P4 readback — 16:13 UTC (25 September 00:13 SGT)

#1075 implementation `52ffc65e038311ad0329514ba656b32467d2ddc5`, merge
`47a0268b57f60ce09760d1837eb7021d151e373a`, Node deployment
`12241632-6379-4867-ae74-3b5bc34ebceb` SUCCESS. Database initialization
391.5 ms on this restart; first scan state writes 1,519.6 ms (previous release
14,831.8 ms with different runtime inputs). First complete loop **133,787 ms**;
its scan profile was 88.4% idle, so faster database work does not establish a
faster complete loop. No causal claim is made for all of the startup difference.

Account report: 81 ms application / 352 ms browser, seven accounts. At
16:13:47Z the fresh protection band was 1,633 ms, no overrun or skipped bands.
Independent broker receipts at 16:12:35.469–36.883Z covered all seven accounts,
32 positions and no missing SL/TP; three live accounts flat. Entry modes,
revision 2, epoch 0 and manual SHADOW policies remained unchanged. Five native
service deployment IDs remained unchanged; no live execution was activated.

A separate startup defect remains: Node's empty in-memory credential map
reconnects and replaces a healthy native checker session on every Node restart,
clearing its readings until another broker pass. The source mechanism and
reproduction are recorded in [the correction contract](v3-independent-startup-2026-09-25.md).
Its local checks pass; production preservation remains Not Verifiable until
this correction deploys and is read back. Full V3 acceptance remains open.

### Later P1/P4 readback — 15:58 UTC (23:58 SGT)

#1074 implementation `704078a9ec91025f947410afabf38fcddf7c6f45`, merge
`fac2a6d6d241b73b074537195f98573aabe85c70`, Node deployment
`5501982a-96f1-4fa1-8eb9-db11d8d7baa2` SUCCESS. First loop 58,563 ms
(prior release 112.5 s); first account report 482 ms application time / 507 ms
Railway proxy time / 734 ms browser elapsed. A later browser reading was 298 ms.
Database-open to BOOT remained 70.65 s: startup acceptance is still Failed.

Independent broker receipts at 15:57:10.370–15:57:11.773Z cover all seven
accounts, 32 open positions and zero missing SL/TP. Account 47790949 had 14
positions, up from 13 at preflight; no entry was submitted by this continuation.
The three live accounts were flat. At 15:58:00Z the protection band reported
3,255 ms, no overrun, and a fresh protection audit. Entry modes, config revision
2, mode epoch 0, manual policies and SHADOW observation remained as before;
no unknown/in-flight entry intents. Existing resting entries remain visible.

[Next bounded correction](v3-guardian-write-batch-2026-09-24.md) batches only
flat-watchlist priority hints and measures startup phases. Its local evidence
does not close cold-start performance or V3 acceptance.

The original baseline and decision sheet below remain chronological records;
the owner continuation above supersedes the former numerical-policy request
and the two missing-target exceptions.

Owner instruction: freeze the eight groups in reply 8,794 and work them to closure.
This register refines the existing P0–P8 completion checklist; it does not replace
the approved revision-3 plan or extend its scope. Baseline main:
`d50ec9a975bc6e59e2fcf54e27240cb6fab47761` (#1069).

## Closure discipline and invariants

A new correction must name a row below, its demonstrated mechanism, a failing
behavioural check, and the evidence needed to close that defect. Reuse passing
evidence unless code, configuration, inputs or scope invalidate it. Keep source
tests, implementation commit, merge commit, deployment, runtime observation and
production acceptance separate. No percentage-complete estimate substitutes
for these gates. Do not re-open retired ordinary producers to manufacture trades.

Maintain account/host/position ownership, valid broker SL/TP, mandatory TP1, human
ownership, entry permissions, risk limits and research thresholds. No fabricated
money, quote timestamps, calendar semantics or successful activity. Missing
evidence remains Not Verifiable, and a observed failure remains Failed until a
correction has appropriate evidence. A passing normal cycle does not erase a
failed load/restart observation.

## The eight frozen groups

| Group | Current evidence | Exact remaining closure condition | Next dependency/action |
|---|---|---|---|
| P0/P3 targets | Failed full TP coverage; 33 positions/all SL/two missing TP at 12:28:26–27Z. Owner selected partial TP1 plus runner; synthesiser still emits null targets. | Approved future-entry target semantics implemented across actual entry paths; fresh broker outcomes for each separately authorised existing-position resolution. | Trigger/fraction/runner/minimum-lot policy values and fresh position-level quotes/constraints. No arbitrary prices. |
| P1/P4 protection performance | Capacity/ownership/preservation fixes retained; #1069 source/CI/deployment passed. Full latency acceptance Not Verifiable. | Single writer, fresh identity/config/quotes, completed protection and broker confirmation meet explicit limits under representative load/recovery; prerequisite reads retain capacity. | Reproduce/isolate synchronous watchdog report work; agree latency limits before grading production samples. |
| P2 accounts/calendars | Account-scoped contracts implemented. Owner confirmed the seven connected accounts (four demo, three live); roster-policy ambiguity closed. Some calendar holiday bounds unknown. | Intended roster and active profiles match their own fresh snapshots/maps/calendar; malformed/absent evidence cannot become open-market permission. | Authenticated current configuration/coverage read; broker evidence for omitted holiday bounds. |
| P5a watchdog delivery | Probe/outbox/retry/observer code retained; independent broker readings succeed. Actual delivery/outage acceptance Not Verifiable. | Sole sender and approved recipient; backlog disposition; durable deduplicated incident/recovery receipts; Node-down and verifier-down detected through independent paths. | Refresh authenticated state, provision approved recipient/credentials/observer, then bounded trials already proposed. |
| P5c scanners | Both services deployed; default and supported EMA/RSI parity and corrected synthetic ingress retained. New feeds remain unconfigured on Node. | Exact intended profile parity and continuous comparison under representative traffic; every input/loss/gap accounted for; bounded resources and protection maintained. | Inventory actual profiles first; port only needed unsupported semantics; scoped observation-only feed trial. |
| P5b/P5d accounting/reporting | Account/population/cashflow and history-link corrections implemented. Historical reconciliation incomplete. | Authenticated account/UI acceptance; broker-proven lifecycle/account/deal matches; attributable balances/cashflows/P&L with unresolved gaps labelled. | Read existing diagnostic responses for AVY/GEV and unpriced/unattributed records; no heuristic merging or invented money. |
| P6/P7 research/configuration | Replay and validation machinery retained. Current eligibility Not Verifiable. | Exact input/profile provenance and existing sample/quality/shadow thresholds; documented supported promotion/sunset/retention decisions. | Audit retained eligible shadow before starting any clock; attributable production replay. Service health alone cannot promote a strategy. |
| P8/maintenance/final acceptance | Six services deployed; recorder storage and runtime capacity acceptance incomplete. | Measured durable allocation/retention/recovery/cost; staged eligible symbol capacity; frozen-scope natural end-to-end evidence and rollback. | Resolve cpp-acct persistence requirement and retention; preceding safety/scanner gates; bounded final test. |

The table groups work rather than asserting eight equally sized tasks. None of
the eight groups is fully accepted at this baseline. Accepted sub-results remain
usable. Source changes to one group cannot silently close another.

## Consolidated owner decision sheet

Only fields that actually affect implementation or consequential operation are
requested. Existing publication/merge approval remains governed by CLAUDE.md.

1. **Momentum target policy:** owner selected **partial TP1 plus runner** during
   this continuation. Remaining values: trigger rule, fraction, minimum-lot
   handling and remaining broker target/terminal-exit rule. The latter requires
   one restart-safe partial manager; writing a target field alone is insufficient.
2. **Existing ETHUSD/XRPUSD exceptions:** after fresh account/position/quote
   readback, specify the replacement target or separately authorised exit for
   each. Future-entry policy does not retroactively choose a position action.
3. **Intended roster/profile scope:** owner confirmed the **seven currently
   connected accounts (four demo, three live)**. This closes the roster-policy
   ambiguity; verify actual identity/coverage and exact strategy/timeframe
   profiles independently. It does not arm another account or strategy.
4. **Operational acceptance:** confirm protection deadlines, retention/restart
   requirements, verified Telegram recipient, sole sender and external observer
   host. Do not paste secrets in chat; use the existing secure provisioning path.

The concrete bounded trials and rollback/abort conditions already exist in
[v3-completion-checkpoint-2026-09-24.md](v3-completion-checkpoint-2026-09-24.md#scoped-release-and-test-proposals).
They cover one labelled incident/recovery, separate maximum-90-second outage
windows, an initial observation-only four-symbol/two-account trial capped at
30 minutes, and a natural-event final test. Their missing identity/configuration
prerequisites must be supplied before presenting them as executable approval.

## Evidence at the freeze

Dated service/resource and raw profile/protection receipts are retained in
[the baseline evidence](evidence/v3-closure-baseline-2026-09-24.json).

- #1069 implementation `6ea440b`, merge `d50ec9a`; merge CI
  [35997723348](https://github.com/ang-kl/bot-trade/actions/runs/35997723348) passed.
- Node deployment `707e9cbd-8768-4671-9821-477f6bf72e5c` succeeded;
  all five native deployment IDs unchanged. Fresh checks at approximately
  12:29Z retain this state.
- Broker receipts at 12:28:26.244Z–12:28:27.692Z cover all seven accounts,
  33 open positions, zero missing SL, two missing TP1.
- Node still has no scanner bridge/feed variable names. Verifier has its
  observation variables but no dedicated Telegram delivery variable names;
  names do not prove effective master/ownership settings.
- Node's one-hour 61-sample Railway metric read: disk peak 4.074 GB on a
  10 GB mounted volume; memory peak 3.909 GB, reported current memory limit
  24 GB. These samples do not prove retention, restart durability, cost or
  future capacity. cpp-acct still has no mounted volume.
- Authenticated application readback remains unavailable after the browser
  connection failed to initialise. Railway logs/status do not replace endpoint
  response bodies. No credentials were extracted as a workaround.
- Requested `.claude/rules/flow.md` is absent from current main and searched
  local locations. Its location was requested; CLAUDE.md is available and read.

## Current corrective work — P1/P4 and P5a

At baseline the `/state/watchdog` handler calls `nodeWatchdogContract` directly.
That reader calls `scannerWork`, which aggregates retained account/session
activity synchronously on the management thread. #1069 startup profiling at
12:15:49Z attributed 9,451.7 ms to scannerWork native reads. Later ordinary
samples attribute approximately 218–249 ms per sampled phase; they do not
erase the startup observation. This is sampled wall time, not proof of CPU
versus storage wait and not broker-confirmation latency.

Correction question: can the same contract be produced through bounded,
read-only worker execution without blocking management, preserving all
timestamps, missing-evidence states, account scope and notification policy?
A stalled worker must yield an explicit unavailable response, and dashboard
report saturation must not consume watchdog's bounded capacity. No new endpoint,
polling job, broker call, database durability setting or trading permission is
needed. Behavioural parity, real HTTP wiring, worker isolation and bounded
failure checks precede any completion claim.

Implemented on the continuation branch: reuse the existing report worker with
one reserved/coalesced watchdog slot in addition to the existing two dashboard
slots. All three remain bounded until actual worker exit; each has the existing
128 MiB V8 old-generation bound. The watchdog retains the existing 15-second
worker deadline and reads a coherent, read-only SQLite snapshot. That deadline
is a resource bound, not a new protection SLA or a guarantee of meeting the
verifier's existing 2-second HTTP deadline. A failed contract returns HTTP 503
with no fabricated healthy work list or observation timestamp.

The two actual HTTP regressions failed before correction and passed afterward.
Focused validation passed 30 checks; three unchanged native scanner checks
were skipped because their binaries are absent. Additional checks prove exact
contract parity, one-flight coalescing, reserved capacity under report saturation,
and event-loop progress during an actual SQLite exclusive lock. Full repository
validation and CI subsequently passed on `41706ea`: 5,436 backend tests, 937
frontend tests, lint, build and colour checks; four native-binary-dependent
backend checks skipped. Six unchanged date-sensitive cases failed under local
Singapore time but passed unchanged in UTC and CI. The updated PR records the
gate for each later head. No production acceptance is claimed.

The owner subsequently approved continued building. The actual watchdog SQL
still scanned all retained decisions to count one account's dispatches; a new
regression reproduced that query plan. A partial covering index on account/time
for `stage='dispatch' AND decision='proceed'` permits an account seek without
changing the existing `julianday` boundaries, timezone parsing or count rules.
It is created through normal database initialization and preserves unattributed history.
No report cache, record deletion, journal-mode change or trading setting is added.

[Local synthetic evidence](evidence/v3-watchdog-query-2026-09-24.json) uses seven
accounts, 250,000 unrelated retained decisions and 1,750 dispatch receipts.
Five complete-contract reads fell from 710–715 ms to 27–31 ms, with every field
identical. Reopening the existing database including index creation took 112 ms;
the database allocated 60 KiB more. A second reopen preserved the result.
These measurements do not establish production latency. The first production
index build scans retained decisions once, and query cost still grows with the
selected account's dispatch history. Controlled release and fresh preflight
remain necessary; this does not close the full P1/P4 or P5a acceptance groups.

## Authenticated continuation — 13:20 UTC

[Dated readback](evidence/v3-authenticated-closure-2026-09-24.json) records the
following accepted sub-results and remaining failures within the same eight
groups. No group is newly declared fully accepted.

- **P1/P4/P5a:** the owner merged #1070 at 13:08:22Z (`80945cb`), with green
  merge CI. Node deployment `bbe66501-5771-411d-a6ee-a5ba1472a849` succeeded
  at 13:09:48Z; native deployment IDs did not change. The authenticated
  pre-startup snapshot was collected after merge, not before it. Through
  13:13:32Z, all 33 account/position/symbol/SL/TP tuples matched exactly;
  seven independent broker receipts advanced, with no unsent, in-flight or
  unknown intents. Two normal loops, book checks and due SGDJPY management
  resumed. Ordinary watchdog samples were 504/503/587 ms with advancing
  timestamps and 60 work receipts. An early missed verifier Node probe and
  later recovery are both retained; no full latency/load SLA is claimed.
- **P2/P5d:** seven-account identity/host coverage and native money readbacks
  passed. Two accounts report verified SGD and withhold `balanceUsd`; five
  report USD. Account switches, momentum configuration, notification settings
  and disabled scanner bridge match the earlier snapshot. Rendered Accounts
  acceptance found six entry-control rows disabled until their unrelated
  broker snapshots loaded. The old first-suffix identity match also fails a
  two-account collision regression. The correction gives authenticated
  engine/readiness responses an explicit routing identity beside the revision.
  Controls and readiness join by exact ID; neither a suffix nor a separately
  polled roster can supply action identity. Missing/duplicate identities refuse
  individual and bulk changes. Default helper reports retain redaction. No additional polling,
  automatic account action, permission or risk-limit change is introduced.
- **P5b/P5d:** retained unknown-P&L diagnosis covers all 22 rows, including
  20 previously written-off rows. AVY/GEV each have two closed ledger rows
  sharing account/position identity; broker lifecycle/deal evidence is still
  required. No row was deleted, attributed, repaired or written off here.
- **P6/P7:** all seven accounts are `UNVALIDATED`, unpinned, and not tick
  ready, although shadow activity is reported. The five latest research
  trials contain zero trades under profile `498439d200112736`, different
  from running shadow profile `967c1defd6e78d09`. Historical shadow summaries
  are unfiltered by required cost schedule and are not promotion evidence;
  elapsed history does not establish the eligible 48-hour window. No clock
  was restarted and no thresholds or profile parameters were changed.

P0/P3 numeric partial-TP1 policy and existing ETH/XRP resolutions remain open.
Calendar bounds, delivery/observer and outage trials, scanner profile/feed
acceptance, account-history reconciliation and recorder durability/capacity
remain the existing closure dependencies. The live recorder reports `/data/tick`
while Railway lists no mounted volume for cpp-acct; recording is not proof of
restart persistence. No new acceptance group or replacement plan was added.
## P1/P4 continuation — account report isolation, 14:02 UTC

#1071 is merged as `e60064c` and deployed. Its seven-account identity correction
passed the full gate and read-only production rendering checks. All 33 broker
account/host/position/symbol/SL/TP tuples and captured settings matched the
preflight; two main-loop and protection-band completions demonstrate subsequent
recovery. The same two missing targets remain open. These are separate results
from the startup failure below.

During that release, `/state/account-engineering` took **61,727 ms** on the
management thread; diagnostics recorded **46,544 ms** event-loop lag and the
protection band recorded a `pnl_watch` budget overrun. Source inspection confirms
the route directly executes the account report's retained-history aggregations.
Later profiler samples also attribute synchronous native reads to
`engineeringView`. This demonstrates a reporting path that can block management;
it does not prove that it caused every observed stall or every database wait.

The correction places that existing report in the existing bounded worker pool,
using a coherent read-only snapshot, two shared report slots, coalescing,
128 MiB worker heaps and the existing 15-second deadline. The separately reserved
watchdog slot is unchanged. Failure returns explicit HTTP 503; it cannot fall back
to management SQL. No report is cached under a newer configuration epoch while
the worker reads an earlier snapshot. Report fields, account rules, timestamps,
query semantics, database durability and trading settings are unchanged. No index
build, broker call, extra polling or worker capacity is added.

Two actual HTTP regressions failed before the correction and passed afterwards.
Focused validation passed 35 tests, including exact output/state preservation,
locked-database event-loop progress, coalescing, pool capacity and retained watchdog
capacity. Full repository and exact-head CI gates remain required before merge.
Post-release acceptance must observe the account report, fresh protection and
normal management; a remaining startup stall stays Failed. The eight groups and
the outstanding partial-TP1 policy values are unchanged.

Rule-source recovery at 14:04 UTC: the owner supplied `INSTALL-PROMPT.md` and
`flow-kit-v0_1_1.zip`. The install prompt was read first. ZIP provenance and hook
version both identify v0.1.1; its `base/.claude/rules/flow.md` exactly matches the
extracted local copy (SHA-256
`9663fb8496f0708ec0f7d36cbcd800a2e98b42d06600942b5fc0d84e005d1515`). The earlier
missing-source limitation is resolved. No installer, hook, permission change or
approval-record write was performed. The kit describes a separate Claude Code
installation; live hook enforcement is Not Verifiable in this Codex task.

## P1/P4 continuation — bounded account-history reads, 14:26 UTC

#1072 passed the full local/exact-head CI gate, merged as `55b286b`, and Node
deployment `9196dd1f-9815-406f-9413-b9e6c03db57d` succeeded. Native deployments
did not change. The cold report hit its 15-second deadline and returned 503.
Initial event-loop samples were 0/1/1,072 ms, but protection timeouts still
occurred; full performance acceptance remains **Failed**. Subsequent reports
returned 200 in 722 and 1,013 ms at 14:19:30Z and 14:22:26Z. Seven fresh broker
receipts preserved all 33 account/host/position/symbol/SL/TP tuples; account,
book, notification, scanner and entry-engine settings matched exactly. Normal
main-loop and protection-band completions advanced. These recovery observations
do not erase the startup failures, slow monitor cadence or AVY/GEV errors.

The remaining report mechanism is demonstrated by SQLite's actual query plan:
both latest-decision aggregates scan retained history and build temporary
GROUP BY trees. The next correction seeks the latest row for each registered
account through two account/time indexes. It retains text timestamp ordering,
the former first-row tie result, decision-before-risk tie priority, and all
unattributed/unregistered evidence. This is the same approved P1/P4 closure
scope, not a new target, risk or account policy.

Paths: `agent/db.js`, `agent/services/account-engineering.js`, their new history
regressions, a reproducible synthetic benchmark and its evidence, this register
and the required CLAUDE serial ledger. The existing report worker and its
deadlines/capacity remain unchanged. Index creation occurs only after account
column migrations. Creation scans each retained history table once and adds
write/retention index maintenance; synthetic timing is not a production budget.

| Maxim / scope | Limit and contrary case | Named evidence |
|---|---|---|
| Preserve the complete account report | Timestamp text and ties retain prior semantics; missing evidence stays null | `latest decisions preserve timestamp ordering, ties, empty accounts and all retained evidence` and existing report/route suites |
| Read only the needed history per registered account | A full retained-history scan or temporary sort fails | `account report seeks history by account without full scans or temporary sorts` checks actual executed SQL plans |
| Keep records and durability through upgrade | Null-account rows cannot disappear; repeat startup must retain output; WAL/FULL cannot weaken | Retained-database and original-accountless-schema upgrade regressions; synthetic row digests |
| Keep protection and trading configuration intact | Any ownership/protection/configuration change fails release acceptance | Fresh seven-account pre/post readback and unchanged native deployment IDs; no trading operations in this change |

The query-plan and two missing-index checks failed before implementation; the
reference output parity check passed before and after. All 24 focused report,
worker, HTTP and migration tests then passed. Full repository/CI gates, measured
migration cost and post-deployment observations remain required before claiming
this correction accepted. Other frozen groups and owner policy gaps remain open.

[Synthetic benchmark evidence](evidence/v3-account-history-query-2026-09-24.json)
contains 250,000 decisions and 250,000 risk events, seven registered accounts
and retained null/unregistered rows. Five complete reports fell from 561–626 ms
to 2.4–4.9 ms with exact output parity. Reopening the retained database, including
both index builds, took 1,665 ms and allocated 20,070,400 additional bytes. Full
row digests matched before/after; a second reopen was idempotent. Reproduce with
`node scripts/benchmark-account-history.mjs`. This is local synthetic evidence,
not a production timing limit. Railway's 14:16:32Z boot reports 3,663 MB database,
28 MB WAL and 5,958 MB free; the subsequent disk metric is 4.072 GB. Production
startup cost remains subject to the controlled release observation.
